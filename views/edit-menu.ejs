<?- include('includes/head.ejs') ?>    
    <?- include('includes/nav.ejs') ?>   
        <main class="container">
            <form action="/admin<?= path ?>" method="POST">
                <div class="form-group row">
                    <label for="name" class="col-sm-2 col-form-label">Menu name</label>
                    <div class="col-sm-10">
                        <input
                            value="<?= menu.name ?>"
                            id="name"
                            class="form-control form-control-sm"
                            type="text" 
                            name="name" 
                            placeholder="Menu name" 
                            required 
                        />
                    </div>
                </div>
                <div class="form-group row">
                    <label for="link" class="col-sm-2 col-form-label">Menu link</label>
                    <div class="col-sm-10">
                        <input
                            value="<?= menu.link ?>"
                            id="link"
                            class="form-control form-control-sm"
                            type="text" 
                            name="link" 
                            placeholder="Menu link e.g: /home" 
                            required
                        />
                    </div>
                </div>
                <div class="form-group row">
                    <label for="order" class="col-sm-2 col-form-label">Menu order</label>
                    <div class="col-sm-10">
                        <input
                            value="<?= menu.order ?>"
                            id="order"
                            class="form-control form-control-sm"
                            type="number" 
                            name="order" 
                            placeholder="Menu order" 
                            min="0" 
                            step="1"
                            required
                        />
                    </div>
                </div>
                <input
                    type="hidden" 
                    name="parentId" 
                    value="<? if (path === '/edit-main-menu') { ?><?= '' ?><? } else if (path === '/edit-sub-menu') { ?><?= menu.mainmenuId ?><? } else { ?><?= menu.submenuId ?><? } ?>"
                    required />
                <input type="hidden" name="id" value="<?= menu.id ?>" />
                <button class="btn btn-block btn-primary" type="submit">Save Menu</button>
            </form>
        </main>
<?- include('includes/end.ejs') ?>
