<?- include('includes/head.ejs') ?>    
    <?- include('includes/nav.ejs') ?>   
        <main class="container">
            <form action="/admin<?= path ?>" method="POST">
                <div class="form-group row">
                    <label for="name" class="col-sm-2 col-form-label">Menu name</label>
                    <div class="col-sm-10">
                        <input
                            id="name"
                            class="form-control form-control-sm"
                            type="text" 
                            name="name" 
                            placeholder="Menu name" 
                            required 
                        />
                    </div>
                </div>
                <div class="form-group row">
                    <label for="link" class="col-sm-2 col-form-label">Menu link</label>
                    <div class="col-sm-10">
                        <input
                            id="link"
                            class="form-control form-control-sm"
                            type="text" 
                            name="link" 
                            placeholder="Menu link e.g: /home" 
                            required
                        />
                    </div>
                </div>
                <div class="form-group row">
                    <label for="order" class="col-sm-2 col-form-label">Menu order</label>
                    <div class="col-sm-10">
                        <input
                            id="order"
                            class="form-control form-control-sm"
                            type="number" 
                            name="order" 
                            placeholder="Menu order" 
                            min="0" 
                            step="1"
                            required
                        />
                    </div>
                </div>
                <input
                    type="hidden" 
                    name="parentId" 
                    value="<? if (parentId) { ?><?= parentId ?><? } else { ?><?= '' ?><? } ?>"
                    required />
                <button class="btn btn-block btn-primary" type="submit">Add Menu</button>
            </form>
        </main>
<?- include('includes/end.ejs') ?>
