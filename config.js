const host = 'localhost';
const dialect = 'mysql';
const username = 'root';
const password = 'admin';
const database = 'gogo';

module.exports = {
    dialect,
    host,
    username,
    password,
    database
};
